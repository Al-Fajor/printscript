package org.example.token;

public enum BaseTokenTypes implements TokenType {
    LET,
    TYPE,
    ASSIGNATION,
    IDENTIFIER,
    SEMICOLON,
    COLON,
    IF,
    ELSE,
    LITERAL,
    OPERATOR,
    PRINTLN,
    SEPARATOR,
    FUNCTION,
}
