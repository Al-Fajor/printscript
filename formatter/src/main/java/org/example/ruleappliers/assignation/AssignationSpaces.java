package org.example.ruleappliers.assignation;

import org.example.ruleappliers.ApplicableSpaces;

public enum AssignationSpaces implements ApplicableSpaces {
	SPACE_BEFORE_ASSIGNATION,
	SPACE_BEFORE_EQUALS,
	SPACE_AFTER_EQUALS,
	SPACE_AFTER_ASSIGNATION
}
