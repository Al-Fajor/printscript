package model;

public enum BaseTokenTypes implements TokenType {
    LET,
    TYPE,
    ASSIGNATION,
    IDENTIFIER,
    SEMICOLON,
    COLON,
    IF,
    ELSE,
    LITERAL,
    OPERATOR,
    PRINTLN,
    SEPARATOR,
}
