package model;

public enum BaseTokenTypes implements TokenType {
    LET_KEYWORD,
    TYPE,
    ASSIGNATION,
    IDENTIFIER,
    SEMICOLON,
    COLON,
    IF,
    ELSE,
    LITERAL,
    BINARY_OPERATOR
}
