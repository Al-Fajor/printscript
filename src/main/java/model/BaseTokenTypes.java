package model;

public enum BaseTokenTypes implements TokenType {
    LET_KEYWORD,
    PRINTLN_KEYWORD,
    TYPE,
    ASSIGNATION,
    IDENTIFIER,
    SEMICOLON,
    COLON,
    IF,
    ELSE,
    LITERAL,
    BINARY_OPERATOR,
}
