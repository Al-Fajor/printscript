package parser.syntax.sentence.strategy;

import model.AstComponent;
import model.Token;

import java.util.List;

public class IfStrategy implements SentenceStrategy {
  @Override
  public AstComponent buildSentence(List<Token> tokens) {
    return null;
  }
}
